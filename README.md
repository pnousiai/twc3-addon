# twc3-addon

This repository contains the Home Assistant add-on in the `modbus-gateway/` directory.

- `modbus-gateway/config.yaml` — add-on manifest
- `modbus-gateway/Dockerfile` — add-on Docker build
- `modbus-gateway/main.py` — Modbus gateway runtime
- `modbus-gateway/run.sh` — add-on startup script
